# Transcribator

Локальный транскрибатор для русского языка с автоматическим определением спикеров. Работает полностью офлайн после загрузки моделей.

## 🎯 Возможности

- **Транскрибация аудио/видео** - поддержка множества форматов (mp3, wav, mp4, avi и др.)
- **Определение спикеров (Speaker Diarization)** - автоматическое разделение речи по говорящим
- **Работает локально** - не требует интернета после первоначальной настройки
- **Оптимизация для CPU** - работает на обычных компьютерах без GPU
- **Множество форматов экспорта** - текст, JSON, SRT, VTT субтитры
- **Поддержка русского языка** - оптимизирована для русской речи

## 📋 Требования к системе

- **Python** >= 3.10 (обязательно!)
- **FFmpeg** - для обработки аудио/видео
- **RAM** >= 8 GB (рекомендуется)
- **Свободное место** ~5 GB (для моделей)
- **ОС**: Windows, Linux, macOS

## 🚀 Установка

### Шаг 1: Установка FFmpeg

#### Windows
```bash
# Через Chocolatey
choco install ffmpeg

# Или скачать с https://ffmpeg.org/download.html
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install ffmpeg
```

#### macOS
```bash
brew install ffmpeg
```

Проверьте установку:
```bash
ffmpeg -version
```

### Шаг 2: Клонирование репозитория

```bash
git clone https://github.com/yourusername/transcribator.git
cd transcribator
```

### Шаг 3: Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
```

### Шаг 4: Установка зависимостей

**⚠️ ВАЖНО: Порядок установки критичен для избежания конфликтов!**

```bash
# Установка в правильном порядке
pip install torch==2.10.0 torchaudio==2.10.0
pip install pyannote.audio==4.0.3
pip install faster-whisper==1.2.1
pip install onnxruntime
pip install pydub ffmpeg-python python-dotenv
```

Или используйте requirements.txt:
```bash
pip install -r requirements.txt
```

### Шаг 5: Получение HuggingFace токена

Для работы определения спикеров необходим токен HuggingFace:

1. Зарегистрируйтесь на [HuggingFace](https://huggingface.co)
2. Перейдите в [Settings → Access Tokens](https://huggingface.co/settings/tokens)
3. Создайте новый токен (Read access достаточно)
4. **Примите лицензии моделей** (важно!):
   - [pyannote/speaker-diarization-3.1](https://huggingface.co/pyannote/speaker-diarization-3.1)
   - [pyannote/segmentation-3.0](https://huggingface.co/pyannote/segmentation-3.0)

### Шаг 6: Настройка конфигурации

Скопируйте `.env.example` в `.env` и укажите ваш токен:

```bash
# Windows
copy .env.example .env

# Linux/macOS
cp .env.example .env
```

Отредактируйте `.env`:
```env
HF_TOKEN=your_actual_token_here
WHISPER_MODEL=small
DEFAULT_LANGUAGE=ru
OUTPUT_DIR=./output
```

## 📖 Использование

### Базовое использование

```bash
python main.py audio.mp3
```

Это создаст файлы в директории `./output/`:
- `audio.txt` - текстовая транскрипция с метками спикеров
- `audio.json` - JSON с метаданными и временными метками
- `audio.srt` - субтитры SRT
- `audio.vtt` - субтитры WebVTT

### Расширенные примеры

```bash
# Выбор конкретных форматов экспорта
python main.py audio.mp3 --formats text json

# Использование более точной модели
python main.py audio.mp3 --model medium

# Указание точного количества спикеров (улучшает качество)
python main.py audio.mp3 --num-speakers 3

# Обработка видео файла
python main.py video.mp4 --output-dir ./results

# Без определения спикеров (быстрее)
python main.py audio.mp3 --no-diarization

# Для английского языка
python main.py audio.mp3 --language en

# Указание диапазона спикеров
python main.py audio.mp3 --min-speakers 2 --max-speakers 5

# Показывать уверенность определения спикеров
python main.py audio.mp3 --show-confidence
```

### Все параметры командной строки

```
Обязательные:
  input_file              Путь к аудио/видео файлу

Основные параметры:
  --output-dir DIR        Директория для результатов (default: ./output)
  --formats {text,json,srt,vtt,all}
                         Форматы экспорта (default: all)
  --model {tiny,base,small,medium,large-v2,large-v3}
                         Размер модели Whisper (default: small)
  --language CODE         Язык транскрибации (default: ru)
  --device {cpu,cuda}     Устройство (default: cpu)

Определение спикеров:
  --no-diarization        Отключить определение спикеров
  --num-speakers N        Точное количество спикеров
  --min-speakers N        Минимум спикеров
  --max-speakers N        Максимум спикеров
  --hf-token TOKEN        HuggingFace токен

Дополнительно:
  --compute-type {int8,float16,float32}
                         Тип вычислений (default: int8 для CPU)
  --min-overlap RATIO     Минимальное пересечение (default: 0.5)
  --show-confidence       Показывать уверенность в текстовом выводе
  --help                  Показать справку
  --version               Показать версию
```

## 🎬 Форматы вывода

### Текстовый формат (`.txt`)

```
[SPEAKER_00] (00:00:05 - 00:00:12)
Это пример транскрибированного текста от первого спикера.

[SPEAKER_01] (00:00:13 - 00:00:20)
А это текст от второго спикера.
```

### JSON формат (`.json`)

```json
{
  "metadata": {
    "source_file": "audio.mp3",
    "duration": 120.5,
    "num_speakers": 2,
    "language": "ru",
    "model": "small"
  },
  "segments": [
    {
      "start": 5.0,
      "end": 12.0,
      "speaker": "SPEAKER_00",
      "text": "Текст сегмента",
      "confidence": 0.95
    }
  ]
}
```

### SRT субтитры (`.srt`)

```
1
00:00:05,000 --> 00:00:12,000
[SPEAKER_00] Это пример транскрибированного текста

2
00:00:13,000 --> 00:00:20,000
[SPEAKER_01] А это текст от второго спикера
```

### WebVTT субтитры (`.vtt`)

Совместимы с YouTube, HTML5 video и большинством современных плееров.

## ⚡ Производительность

### Время обработки (CPU, модель small)

| Длительность аудио | Время обработки | Скорость |
|-------------------|----------------|----------|
| 5 минут           | ~2 минуты      | 2.5x     |
| 30 минут          | ~10 минут      | 3.0x     |
| 1 час             | ~20 минут      | 3.0x     |

**Примечание**: На GPU обработка в 10-20 раз быстрее.

### Требования к памяти

| Модель  | RAM (CPU) | VRAM (GPU) | Точность |
|---------|-----------|------------|----------|
| tiny    | ~1 GB     | ~1 GB      | ⭐⭐      |
| base    | ~1 GB     | ~1 GB      | ⭐⭐⭐    |
| small   | ~2 GB     | ~2 GB      | ⭐⭐⭐⭐  |
| medium  | ~5 GB     | ~5 GB      | ⭐⭐⭐⭐⭐ |
| large   | ~10 GB    | ~10 GB     | ⭐⭐⭐⭐⭐ |

**Рекомендация**: Для русского языка модель `small` дает оптимальное соотношение качества и скорости.

## 🔧 Troubleshooting

### Проблема: Ошибка при импорте модулей

**Симптом**: `ModuleNotFoundError: No module named 'faster_whisper'`

**Решение**: 
```bash
# Убедитесь что виртуальное окружение активировано
# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: Конфликт ONNX Runtime

**Симптом**: Одна из библиотек работает очень медленно

**Решение**: Установите зависимости в правильном порядке:
```bash
pip uninstall onnxruntime onnxruntime-gpu -y
pip install torch==2.10.0 torchaudio==2.10.0
pip install pyannote.audio==4.0.3
pip install faster-whisper==1.2.1
pip install onnxruntime
```

### Проблема: Ошибка авторизации HuggingFace

**Симптом**: `401 Unauthorized` или `Access denied`

**Решение**:
1. Проверьте что токен правильный в `.env` файле
2. Убедитесь что вы приняли лицензии моделей:
   - https://huggingface.co/pyannote/speaker-diarization-3.1
   - https://huggingface.co/pyannote/segmentation-3.0
3. Токен должен иметь Read access

### Проблема: Нехватка памяти

**Симптом**: `OutOfMemoryError` или `Killed`

**Решения**:
- Используйте меньшую модель: `--model tiny` или `--model base`
- Закройте другие программы
- Обрабатывайте файлы по частям
- Увеличьте swap (Linux)

### Проблема: FFmpeg не найден

**Симптом**: `FileNotFoundError: ffmpeg not found`

**Решение**:
- Установите FFmpeg (см. раздел Установка)
- Убедитесь что FFmpeg в PATH:
  ```bash
  ffmpeg -version
  ```
- Windows: Перезапустите терминал после установки

### Проблема: Плохое качество для русского языка

**Решения**:
- Используйте модель побольше: `--model medium`
- Убедитесь что указан правильный язык: `--language ru`
- Проверьте качество исходного аудио (желательно 16kHz+)
- Используйте `--vad-filter` для фильтрации тишины

### Проблема: Неправильное определение количества спикеров

**Решения**:
- Укажите точное количество: `--num-speakers 3`
- Или диапазон: `--min-speakers 2 --max-speakers 4`
- Убедитесь что спикеры хорошо различимы (разный тембр голоса)

## 🔍 FAQ

**Q: Могу ли я использовать для других языков?**  
A: Да! Whisper поддерживает 99 языков. Используйте параметр `--language`, например `--language en` для английского.

**Q: Нужен ли интернет для работы?**  
A: Интернет нужен только при первом запуске для загрузки моделей (~5 GB). После этого все работает офлайн.

**Q: Можно ли использовать GPU для ускорения?**  
A: Да, если у вас NVIDIA GPU с CUDA: `--device cuda`. Это ускорит обработку в 10-20 раз.

**Q: Какой формат аудио лучше всего подходит?**  
A: Любой. Скрипт автоматически конвертирует в нужный формат (WAV, 16kHz, моно).

**Q: Сколько спикеров может определить?**  
A: Теоретически без ограничений, но качество снижается при >5 спикерах. Лучше работает с 2-4 спикерами.

**Q: Можно ли улучшить качество транскрипции?**  
A: Да:
- Используйте лучшее качество исходного аудио
- Выберите модель побольше (`medium` или `large`)
- Убедитесь что указан правильный язык
- Используйте `--num-speakers` если знаете количество

**Q: Как обработать длинный подкаст (2+ часа)?**  
A: Скрипт автоматически обрабатывает файлы любой длины. Для 2 часов на CPU потребуется ~40-50 минут.

## 📊 Технические детали

### Используемые технологии

- **faster-whisper 1.2.1** - оптимизированная транскрибация (CTranslate2)
- **pyannote.audio 4.0.3** - определение спикеров
- **PyTorch 2.10** - ML backend
- **pydub + FFmpeg** - обработка аудио

### Точность для русского языка

- **Whisper small**: WER ~10-15% (Word Error Rate)
- **Whisper medium**: WER ~8-12%
- **Speaker Diarization**: DER ~10-15% (Diarization Error Rate)

## 🤝 Вклад в проект

Приветствуются pull requests! Для крупных изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT

## 🙏 Благодарности

- [OpenAI Whisper](https://github.com/openai/whisper) - модель транскрибации
- [faster-whisper](https://github.com/SYSTRAN/faster-whisper) - оптимизированная реализация
- [pyannote.audio](https://github.com/pyannote/pyannote-audio) - определение спикеров

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел Troubleshooting
2. Посмотрите существующие [Issues](https://github.com/yourusername/transcribator/issues)
3. Создайте новый Issue с подробным описанием проблемы

---

**Сделано с ❤️ для русскоязычного сообщества**
